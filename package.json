{
  "name": "satellite-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Uptime and render monitoring for a fleet of ticketing sites, with Telegram alerts.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "render": "node scripts/render.mjs",
    "lint": "node --check scripts/check.mjs && node --check scripts/render.mjs && node --check scripts/lib/storm.mjs && node --check scripts/lib/control.mjs && node --check scripts/lib/probe.mjs",
    "test": "node --test \"scripts/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
